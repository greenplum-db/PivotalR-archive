## Wrapper function for MADlib's decision tree

madlib.dt <- function(formula, data, weights, subset, 
                      na.action = na.rpart, method, params,
                      control, ...)
{
    
}
